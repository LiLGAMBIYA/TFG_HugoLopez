{% extends 'base.html.twig' %}

{% block title %}Nueva Factura - JM Motor{% endblock %}

{% block body %}
<div class="row justify-content-center animate-fade-in mt-4">
    <div class="col-md-8">
        <div class="glass-card p-5" style="border-top: 4px solid var(--accent-blue);">
            <h1 class="mb-1">🧾 Nueva <span style="color: var(--accent-blue);">Factura</span></h1>
            <p class="text-muted mb-4">Genera una nueva factura asociada a una cita.</p>

            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_errors(form) }}

                {% for child in form %}
                    <div class="mb-3">
                        {{ form_label(child) }}
                        {{ form_widget(child, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(child) }}
                    </div>
                {% endfor %}

                <div class="d-flex gap-3 mt-4">
                    <button class="btn btn-primary flex-grow-1">💾 Generar Factura</button>
                    <a href="{{ path('app_factura_index') }}" class="btn btn-outline-primary">Cancelar</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
