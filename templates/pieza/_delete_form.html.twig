<form method="post" action="{{ path('app_pieza_delete', {'id': pieza.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pieza.id) }}">
    <button class="btn">Delete</button>
</form>
