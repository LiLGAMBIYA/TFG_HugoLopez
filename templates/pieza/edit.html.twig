{% extends 'base.html.twig' %}

{% block title %}Editar Pieza - JM Motor{% endblock %}

{% block body %}
<div class="row justify-content-center animate-fade-in mt-4">
    <div class="col-md-8">
        <div class="glass-card p-5" style="border-top: 4px solid var(--accent-orange);">
            <h1 class="mb-1">✏️ Editar <span style="color: var(--accent-orange);">Pieza</span></h1>
            <p class="text-muted mb-4">Modifica los datos de la pieza en el inventario.</p>

            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_errors(form) }}

                {% for child in form %}
                    {% if child.vars.name != '_token' %}
                    <div class="mb-3">
                        {{ form_label(child) }}
                        {{ form_widget(child, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(child) }}
                    </div>
                    {% endif %}
                {% endfor %}

                <div class="d-flex gap-3 mt-4">
                    <button class="btn btn-primary flex-grow-1">💾 Guardar Cambios</button>
                    <a href="{{ path('app_pieza_index') }}" class="btn btn-outline-primary">Cancelar</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
